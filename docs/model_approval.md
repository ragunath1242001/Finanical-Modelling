# Model Approval

Approval decisions include:

- Approved;
- Conditionally Approved;
- Rejected;
- Deferred.

Conditional approval requires explicit conditions, use restrictions, expiry or review date, required remediation and next review date.

A rejected model must not enter production. A production model requires valid approval. A retired model version cannot return to production without a new version.
